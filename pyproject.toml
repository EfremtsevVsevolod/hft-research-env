[project]
name = "hft"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "sortedcontainers",
    "pyyaml",
    "pandas",
    "pyarrow",
    "websockets",
]

[project.optional-dependencies]
test = ["pytest"]

[tool.setuptools.packages.find]
include = ["src*"]
